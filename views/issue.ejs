<!DOCTYPE html>
<html>
  <head>
    <%- include ../common/bootstrap.ejs %>
    <title></title>
    <script>
    function showKeyCode(event) {
      event = event || window.event;
      var keyID = (event.which) ? event.which : event.keyCode;
      if( ( keyID >=48 && keyID <= 57 ) || ( keyID >=96 && keyID <= 105 ) || ( keyID == 8) || ( keyID == 9)|| ( keyID == 46))
      {
        return;
      }
      else
      {
        swal({
         type: 'error',
         width: '40rem',
         title: '입금액에 숫자만 입력하세요.',
         text: 'Something went wrong!',
      })
        return false;
      }
      /* 48~57:일반 숫자키 코드, 96~105:숫자키패드 숫자키 코드 */
    }
  </script>
  </head>
  <body>
    <%- include ../common/header.ejs %>
    <div class="container">



      <form class="form-horizontal" action="/issue" method="post">

        <div class="form-group">
          <label for="formID"><%= i18n.issue_label1%></label><span style="color:red"> *</span>
          <input type="text" class="form-control" name="emailTag" value="" required>
        </div>

        <div class="form-group">
          <label for="amountID"><%= i18n.issue_label_coin%><span style="color:red"> *</span></label>
          <input type="number"  pattern="[0-9]*" inputmode="numeric" class="form-control" id = "amountID" name="amount" value="" onkeydown="return showKeyCode(event)" required>
        </div>

        <div class="form-group">
          <label for=""></label>
          <input type="hidden" name="adminAddr" class="form-control" value="<%=adminAddr%>">
        </div>

        <div class="form-group">
          <label for="memoID"><%= i18n.sendAsset_memoID%></label>
          <input type="text" class="form-control" id = "memoID" name="memo" value="">
        </div>

        <div class="form-group">
          <label for="icoTag">적요</label>
          <input type="text" class="form-control" placeholder="" name="icoType" value="<%=icoType%>">
        </div>

        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
            <button type="submit" class="btn btn-success pull-right asset"><%= i18n.issue_btn_confirm%></button>
			<text class="pull-right">&nbsp&nbsp&nbsp&nbsp</text>
            <button type="button" class="btn btn-default pull-right asset" href="location.href='/'"><%= i18n.issue_btn_cancel%></button>
          </div>
        </div>

      </form>
    </div>
  </body>
</html>
